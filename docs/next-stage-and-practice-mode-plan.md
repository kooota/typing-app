# 次ステージ��線と実践モード追加計画

## Summary

- クリア画面に「次のステージへ」ボタンを追加し、通常ステージは�らずそのまま次へ進めるようにする。
- 新しく「実践モード」を追加し、五十音カリキュラムとは別に、やさしい既成単語を 10 問連続で入力させる。
- 実践モードは最初から解放し、通常ステージ進行とは独立して��べるようにする。
- 実践モードの結果は **ノー��スで終えた問題数** を `X / 10` として表示する（��点定義は後述「実践の��点」参照）。各回の履歴を `localStorage` に複数件保存する。
- 実践モ�線と履歴の要約はホーム画面に常設する。

## Key Changes

### クリア画面の次ステージ��線

- `Result` 画面に「トップにもどる」に加えて「次のステージへ」ボタンを追加する。
- `nextStageId(stageId)` が存在する場合だけ「次のステージへ」を表示し、押下で `/play/:nextStageId` に��移する。
- 最終通常ステージでは現状どおり次リンクは出さず、休憩メッセージのみ表示する。
- 既存の `location.state` 依存は��持し、直ア�動を変えない。

### 実践モードのデータ定義

- 通常ステージ定義とは別に、実践モード��用の単語セットを新設する。
- 単語は五十音カリキュラムから離れた「意味のあるやさしい単語」を固定配列で管理する。
- 初期実装では 10 問／回を成立させるため、最低 15〜20 語程度を用意し、各回はその中から重複なしで 10 問を抽出する。
- ローマ字表記は実践モード用に単一路に固定する。
  - `し -> si`
  - `ち -> ti`
  - `つ -> tu`
  - `�� -> hu`
  - `を -> wo`
  - `ん -> n`
- 実践モードでも画面上のお手本表示は `answer` をそのまま見せ、`acceptedAnswers` も `answer` と同じ 1 通りのみを持つ。
- 実践モードは最初から全単語を出題対象に�のかなを含む単語も制限しない。

### 実践の��点（ノー��ス）

- **1 問の「ノー��ス」**: その問題について、**��キー入力が 1 回もない**状態で最後まで打ち切れたとき、その�ス 1 件として数える。
- **��キーが 1 回でもあった問**は、最終的に正しく打ち終えて次へ進めても **ノー��スには含めない**（通常ステージの「1 問あたりミス」の考え方に近い）。
- 10 問中も同じ問題内に留まり、正しく完走するまで進めない前提のため、「最終的に打てた問数」だけを数えると常に 10 になり指標にならない�のきつさを残す指標としてノー��ス数を��用する。**
- 結果画面・ホームの文言は **「せいかい」ではなく「ノー��ス」** とし、��足で「まちがいなしでおわったもんだい�えている」旨を短く示す。

### ルー��ィングとプレイフロー

- 実践モード用に��用ルートを追加する。
 �: `/practice`
  - 結果表示も通常ステージと分ける。
  - 推��: `/practice/result`
- 実践モードの 1 回は常に 10 問固定にする。
- 通常 `Play` を無理に兼用せず、実践モードは��用ページとして分ける。
- 実践モード結果画面は通常の `Result` を流�用ページにし、通常結果画面の `location.state` 欠如時リダイレクトと同様のガードを持たせる。
- 実践モード結果画面は `useParams` に依存せず、`/practice/result` に `PracticeSessionResult` を `location.state` で渡して描画する。
- 実践モードでは星評��を使わず、**ノー��ス完答数**だけを結果とログに載せる。
- 正解時のテンキー、キ�イド、読み上げ、効果音の基本動作は通常プレイとそろえる。
- 実践モードの読み上げも `Question` の既存ルールを流用し、`voiceText` と `voiceFirstChar` を使って単語全体を読んでから入力開始点を��助する。
- ��答時は通常プレイ同様にその問題内へ留まる。
�のかな�しいまちがいが増える可能性は初期実装では許�答でもスキップできるモード」は実機テスト後の調整候��とする。

### 保存データとログ

- `Progress` とは別に、実践モード履歴を保存する構造を追加する。
- 保存内容は少なくとも以下を持つ。
  - `playedAt`: 実行日時の ISO 文字列
  - `questionCount`: 固定で `10`
  - `correctCount`: `0..10`。**意味はノー��スで終えた問題数**（「正解数」や「完走した問数」とは別。フィールド名は JSON 互換のため `correctCount` のままにしてよいが、型・UI ではノー��スと明示する）
  - `wordIds`: その回に出た問題 ID 一覧
- 保存先は既存の `settings` / `progress` と分けた新キーにする。
  - 例: `typing-app/v1/practice-log`
- 実践ログ保存は既存 storage の方���し、`localStorage` 不可時はメモリ保持にフォールバックする。
- 保存形式は新しい順の配列にし、ホーム表示と将来の��張に使いやすくする。
- `appendPracticeLog()` は新規履歴を先頭に追加し、20 件を超えたら末尾の古い履歴から切り��てる。
- `resetAllStorage()` 実行時は実践モード履歴も同時に削除する。

### ホーム画面 UI

- ホームに通常ステージ選択とは別の「実践モード」開始��線を追加する。
- 現在のホームにある通常ステージ一覧 UI はそのまま��持し、その下か近接位置に「実践モード」開始ボタンを置く。
- ホームには実践ログの要約を表示する（数値はすべて **ノー��ス** の意味）。
  - 直近: 「前回ノー��ス 7 / 10」のように明示
  - ベスト: 「ベスト（ノー��ス）9 / 10」など
  - 必要なら履歴 3 件程度をリストに表示（各行もノー��ス表記）
- 実践モードの説明文に、**ノー��スでか��える**旨を短く書く。
- ログが 0 件のときは履歴一覧を出さず、「まだきろくがないよ」のような空状態メッセージを表示する。
- ログの常設場所はホーム画面に限定し、設定画面には出さない。
- 実践モード開始ボタンの近くに、「おとなといっしょに あそべるよ」のような短い��助文を添えることは任意の改善項目とする。

## Public Interfaces / Types

- `StageId` は通常カリ�用の�持し、実践モードを `StageId` に含めない。
- 問題データ構造は既存の `Question` を流用し、実践モード��用の `PracticeQuestion` は新設しない。
- 実践モード固有の型だけを追加する。
  - `PracticeSessionResult`
  - `PracticeLogEntry`（`correctCount` は **ノー��ス完答数** の意味で JSDoc 等で固定する）
- 保存 API も通常進行と分ける。
  - `loadPracticeLog()`
  - `appendPracticeLog()`
- 既存の `saveProgress` / `loadProgress` の��務は変えず、通常ステージ進行だけを��う。

## Test Plan

- 通常ステージ結果画面で、次ステージがある場合のみ「次のステージへ」が表示され、押下で次の `/play/:stage�移すること。
- 最終通常ステージでは次リンクが表示されないこと。
- 実践モード開始で 10 問出題され、11 問目に進まず�移すること。
- 実践モード結果画面�ス `X / 10`** が表示され、文言が「正解数」と��解されないこと。
- 実践モード結果画面へ `state` なしで直接アクセスした場合はトップへ��ること。
- 実践モード終了時に履歴が 1 件追加され、日時・出題数・�ス数（`correctCount`）**・出題 ID が保存されること。
- 実践モード履歴は新しい順に保持され、21 件目追加時に最古の 1 件が��てられること。
- ホーム画面で実践モード開始ボタンが見え、直近・ベストが **ノー��ス** として保存内容から復元されること。
- ホーム画面でログ 0 件のときは空状態メッセージが表示されること。
- `resetAllStorage()` 後に通常進行だけでなく実践履歴も消えること。
- 実践ログ保存が `localStorage` 不可時でもメモリモードで破��しないこと。
- 既存の通常ステージ進行、解放、星評��、設定保存が回 regressしないこと。
- `npm test` と `npm run build` が通ること。

## Assumptions

- 実践モードの単語は外部取得せず、コード内の固定配列で管理する。
- 実践モードの単語は子ども向けのやさしい�を想定し、初期実装では 2〜4 文字程度を中心にする。
- 実践モードは通常ステ�順とは独立し、最初から全単語が出題される。
- 初期語�数は 15〜20 語程度を想定し、抽選の偏りや前回との高い重複は初期実装では許容する。
- 単語抽出は各回 10 問の重複なしまでを保証し、前回セッションとの重複回避までは初期実装で行わない。
- 実践履歴はホームで見る��易ログで十分とし、��用の履歴一覧ページは初期実装では作らない。
- **��点はノー��ス完答数に固定する。**「最終的に打てた問数」ベースへの変更は別判断とする（10 問完走型ではその指標は常に 10 に近くなりやすい点に注意）。
